<UserControl x:Class="Lebenslauf.App.Presentation.Shell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Lebenslauf.App.Presentation">

    <Grid Background="{ThemeResource BackgroundBrush}"
          AutomationProperties.AutomationId="Shell.Root">

        <ContentControl x:Name="NavigationContent"
                        AutomationProperties.AutomationId="Shell.NavigationContent"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch" />

    </Grid>
</UserControl>
